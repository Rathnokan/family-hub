/**
 * Family Hub Card - Shared CSS (barrel).
 * Real content lives in css/part*.js (byte-identical slices, concatenated
 * by css/index.js). Re-exported so `import { CSS } from "./css.js"` still works.
 */
export { CSS } from "./css/index.js";
